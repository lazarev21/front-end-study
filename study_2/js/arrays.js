arr = [3, 4, 6, -1, 1, -2]
let maxElementArray = 0;

for (let i = 0; i < arr.length; i++) {
if (arr[i] > maxelementarray) {        maxElementArray = arr[i];
    }

}
console.log(maxElementArray)

/* function sumInput () {
    let input 
    let inputArray = []

    while (true) {
        input = prompt()

        if (input === '' || input === null || !isFinite(input)) break;
  
    inputArray.push (+input);
    }

    let sum = 0
        for (let key of inputArray) {
        sum += key;
    }
return sum
}


alert (sumInput())
 */


/* let style = ['Джаз', 'Блюз']

style.push ('Рок-н-ролл')

middleArray = ((Math.round(style.length/2)) - 1)

style[middleArray] = 'Классика'


console.log (style.shift())
style.unshift ('Реп', 'Регги')
console.log (style)
 */